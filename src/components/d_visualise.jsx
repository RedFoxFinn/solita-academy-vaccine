import React from 'react';

const DataVisualisation = (props) => {
  const {vaccinations, vaccines} = props;
  return <section id='dataVisualisation' >
    <p>data visualisation</p>
  </section>
};

export default DataVisualisation;